{
  "name": "cortex-vault",
  "version": "1.0.0",
  "description": "MCP server for structured Obsidian vault operations",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "js-yaml": "^4.1.0"
  }
}
